%% FDCalibrate: 10s dynamic speed-density relation built on reconstructed data (sampling frequency=1Hz)
function [a, b] = SDCalibrate(aggLoopData)
	%%local trash no change in remote version
	a = 0;
	b = 0;
end
